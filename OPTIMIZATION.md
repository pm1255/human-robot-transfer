# 为什么现有替换效果不好，下一步怎样做

## 基于当前代码和结果的诊断

1. 人→机器人：`v2/render_conversion.py` 使用逐帧 OpenCV Telea 修补启发式人体/手掩码，再把 URDF 网格渲染贴回。没有跨帧背景建模，没有场景深度；遮挡、接触、相机尺度不可信。较复杂人体姿态的 IK 拟合也不等于机器人可执行轨迹。
2. 机器人→人：VACE 接收掩码、参考图和描述，但没有目标人手逐帧骨架/深度。夹爪只有两指，不能唯一决定人的五指。源视频 256²、5 FPS，放大无法恢复真实手指和接触细节。
3. 扩大掩码可缓解腕部被裁切，也会侵入物体边缘。参考图仅提供外观，种子筛选没有建立物理约束。更大模型也会把机器人重新画回来。
4. v4 训练目标是动作和未来视频预测，不能简单交换输入输出就获得双向编辑能力。

## 推荐顺序（尚未实现/验证）

| 阶段 | 做法 | 判定标准 |
|---|---|---|
| 无训练基线 | 找回高分辨率源视频；SAM2 全时段编辑掩码与独立物体保护掩码；拆分接近/抓住/移动/释放 | 物体不被擦除，机械臂无残留 |
| 几何条件 | 人→机器人用 URDF IK + 相机标定生成深度/轮廓；机器人→人用手模型拟合抓握姿态，约束拇指/食指与物体接触 | 几何可行、接触保持、左右手和骨长连续 |
| 时序生成 | 将骨架/深度作为明确条件，局部区域视频编辑；重叠窗口维持同一手/机器人外观 | 无手指融化、腕部翻转、机器人部件漂移 |
| 小规模适配 | 若条件正确仍持续画错本体，用针对目标手/机械臂的 LoRA；训练外观和运动/接触条件，分开消融 | 独立场景优于原模型；不要只报告训练场景 |
| 配对准入 | 物体轨迹、接触阶段、背景变化、几何失败和人工评审联合筛选，最后做下游策略对照 | 图像更像不等于动作等价 |

结论：**不必一开始全量微调，但要追求稳定的大规模数据生产，定向适配通常值得做；微调不能代替几何和接触约束。** 先固定源场景、参考图和随机种子，对照各条件的贡献，再决定训练投入。

## 可参考的专门方法

- [H2R-Grounder](https://arxiv.org/abs/2512.09406)：从非配对机器人视频训练，通过清背景、夹爪位置/朝向视觉提示，微调 Wan2.2。无需人机配对训练数据，不等于无需训练。对操作机械臂比全身换装更贴合。
- [OmniHumanoid](https://github.com/showlab/OmniHumanoid)：机器人外观 P1 LoRA + 动作迁移 P2，提供 G1/H1 等适配。主要面向全身 humanoid 视频；不能直接据此保证桌面手指抓握准确。
- [VACE 官方使用说明](https://github.com/ali-vilab/VACE/blob/main/UserGuide.md)：参考图、掩码和其他控制组合的基础能力，不能视为跨本体物理配对验证。

本次没有训练上述专门模型，也没有把他人演示作为我们的结果。
